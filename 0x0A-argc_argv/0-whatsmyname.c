#include <stdio.h>
/**
 * main - writes its name
 * @argc: argument count
 * @argv: array of pointers to argument string
 * Return: void
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
